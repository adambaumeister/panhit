# Database #
# Database allows panhit to run using multiprocessing, by storing data on disk instead of in-mem.
# Example
# db:
#  type: JsonDB
#  path: database
db:
  type: JsonDB
  path: database

configdb:
  type: JsonDB
  path: configdb